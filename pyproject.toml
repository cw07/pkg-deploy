[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "package-deploy-new"
version = "0.1.3"
description = "Modern Python Package Deployment Tool"
requires-python = ">=3.8"
dependencies = [
    "toml",
    "tomlkit",
    "build",
    "twine",
    "Cython"
]

[tool.setuptools]
package-dir = {"" = "src"}
zip-safe = false

[tool.setuptools.packages.find]
where = ["src"]